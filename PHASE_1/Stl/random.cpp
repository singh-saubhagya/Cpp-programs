#include<bits/stdc++.h>
using namespace std;
signed main(){
	vector<vector<pair<int,int >>> temp;
	temp.resize(4);
	temp[0].push_back({0,0});
	auto it=temp[0].begin();
	it->second++;
	cout<<it->first<< " "<< it->second<<endl;

}